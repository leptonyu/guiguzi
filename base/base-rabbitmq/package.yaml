_common: !include "../../common.yaml"
name:                base-rabbitmq
<<: *legal

dependencies:
  - base >= 4.11 && < 5
  - boots-app
  - salak
  - network
  - amqp


library:
  source-dirs: src
  exposed-modules:
  - Base.Rabbitmq
